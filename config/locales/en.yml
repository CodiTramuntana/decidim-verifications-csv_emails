en:
  activemodel:
    attributes:
      csv_email_authorization_handler:
        email: Email
  decidim:
    authorization_handlers:
      csv_email:
        name: By email
      csv_email_authorization_handler:
        explanation: Authorize your user account against the Email
        name: By email
        type: Excel
    verifications:
      csv_email:
        admin:
          csv_verifications:
            create:
              success: Successfully imported %{count} items (%{errors} errors)
            destroy_all:
              success: All emails data have been deleted
          destroy:
            title: Delete all emails data
            confirm: Delete all the emails can not be undone. Are you sure you want to continue?
          index:
            data: There are %{count} records loaded in total. Last upload date was on %{due_date}
            empty: There are no emails data. Use the form below to import it using a CSV file.
            title: Current emails data
          instructions:
            body: To do this you must enter system administration and add the excel or combined authorizations to the organization
            title: You need to activate the email module for this organization
          new:
            info: 'Must be a file generated by excel and exported with CSV format with one column: email'
            file: Excel .csv file with emails data
            submit: Upload file
            title: Upload a new emails
        authorizations:
          create:
            error: Unable to verify user
            success: User successfully verified.
          new:
            send: Send
            sending: sending
            title: Verify my user
        errors:
          messages:
            not_in_csv: We could not find your email in our database of members. If the email entered is correct and the problem persists, please, contact an administrator.
            not_same_email: Email entered is not the same as the user logged in
